<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/c7867bb742.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="./css/styles.css">
    <title>Document</title>
</head>
<body>
    <header>
        <h1>The Bars List</h1>
        <div class="message"><%= message %></div>
        <form method="POST" action="addBar">
            <input type="text" placeholder="Bar" name="barLyrics">
            <input type="text" placeholder="Rapper Name" name="barRapper">
            <input type="text" placeholder="Song" name="barSong">
            <button id="submit-bar" type="submit">Submit</button>
        </form>
    </header>

    <div class="barsList">
        <ul class="bars">
            <% for (let i = 0; i < bars.length; i++) { %>
                <li class="bar flex flex-horizontal">
                    <div class="likes-dislikes flex flex-vertical flex-votes">
                        <!--upvote-->
                        <div class="like" value=<%= bars[i].barLikes %>>
                            <i class="fa-solid fa-angle-up"></i>
                        </div>
                        <!--total likes/dislikes-->
                        <span class="votes"><%= bars[i].barLikes - bars[i].barDislikes %></span>
                        <!--downvote-->
                        <div class="dislike" value=<%= bars[i].barDislikes %>>
                            <i class="fa-solid fa-angle-down"></i>
                        </div>
                    </div>
                    <figure class="flex flex-vertical flex-content">
                        <blockquote><%= bars[i].barLyrics %></blockquote>
                        <figcaption>
                            <!--&mdash; --><span class="barRapper"><%= bars[i].barRapper %></span>,
                            <cite class="barSong"><%= bars[i].barSong %></cite>
                        </figcaption>
                    </figure>
                </li>
            <% } %>
        </ul>
    </div>
    <script src="./scripts/main.js"></script>
</body>
</html>